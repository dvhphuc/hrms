type Query {
    users : [User]
    usersInPage(page: Int!, size: Int!): [User]
    user(id: ID!): User
}

type User {
    id: ID!
    username: String!
    password: String
    createdAt: String
    status: Boolean!
    role: Role!
}

type Role {
    id: ID!
    name: String
}